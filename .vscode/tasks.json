{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Maven Clean and Install",
            "type": "shell",
            "command": "mvn",
            "args": ["clean", "install"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Runs Maven clean and install to ensure the project is built before running."
        }
    ]
}
